package org.depies.ml.ml4j;

import org.nd4j.linalg.api.ndarray.INDArray;

public class OneRule {
	int featureIndex, featureSplit;
	
	public void fit(INDArray features, INDArray labels, int[] featureOrder) {
		for(int i : featureOrder) {
			
		}
	}
}
